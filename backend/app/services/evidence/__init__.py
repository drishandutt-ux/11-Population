"""Evidence gathering: web search, page reading, Reddit listening, judges, and the research loop.

Ported from SuperMind (TypeScript) — see PRODUCT_BLUEPRINT §10a. Pure of the database except
`loop.py`, which persists rows and publishes WebSocket events.
"""
